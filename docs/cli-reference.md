# CLI Reference

This page has moved. The canonical CLI reference is at
[reference/cli.md](reference/cli.md). It lists every `furlow` subcommand
(`init`, `start`, `dev`, `validate`, `add`, `build`, `export`) with up to
date options and behavior.
